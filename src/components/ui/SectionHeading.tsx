import type { ReactNode } from "react";
export function SectionHeading({ eyebrow, title, children, align = "center" }: { eyebrow: string; title: string; children?: ReactNode; align?: "center" | "left" }) { const parts = title.split("|"); return <div className={`mb-12 ${align === "center" ? "text-center" : "text-left"}`}><p className="mono mb-3 text-xs font-bold uppercase tracking-widest text-green-700">{eyebrow}</p><h2 className="mono text-3xl font-extrabold tracking-tight text-black sm:text-5xl dark:text-white">{parts.map((part, i) => <span key={part} className={i % 2 ? "text-sky-500" : ""}>{part}</span>)}</h2><div className={`section-underline mt-5 ${align === "center" ? "mx-auto" : ""}`} />{children && <p className="mx-auto mt-5 max-w-2xl text-sm leading-6 text-slate-600 dark:text-slate-300">{children}</p>}</div>; }
